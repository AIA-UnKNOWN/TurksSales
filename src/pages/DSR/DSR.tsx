import React from 'react';
import {
  View,
  Text,
  ScrollView,
} from 'react-native';

const DSR = () => {
  return (
    <ScrollView>
      <View>
        <Text>This is DSR Screen</Text>
      </View>
    </ScrollView>
  );
}

export default DSR;